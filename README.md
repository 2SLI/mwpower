# MW Power (Vite + Firebase)

## Run

```bash
npm install
cp .env.example .env
npm run dev
```

## Firebase setup

1. Open Firebase Console -> Project settings -> Your apps -> Web app config.
2. Copy values into `.env` (`VITE_FIREBASE_*` keys).
3. Restart the dev server after editing `.env`.
